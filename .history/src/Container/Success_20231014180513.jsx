/* eslint-disable react/prop-types */
export function Success({userArray}) {
  return (
    <>
      <div className="close success">
      {/* {userArray.map(user=>{
        const {fName, lName, email} = user
        return(
          <>
          <h4>{fName}</h4>
          <h4>{lName}</h4>
          <h4>{email}</h4>
          </>
        )
      })} */}
        <p></p>
      </div>
    </>
  );
}
